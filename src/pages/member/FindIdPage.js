import React from 'react';
import FindIdContainer from "../../containers/member/FindIdContainer";

function FindIdPage({history}) {
    return(
        <FindIdContainer history={history}/>
    );
}

export default FindIdPage;